import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.vmillet.chatterbox',
  appName: 'chatter-box',
  webDir: 'dist/browser'
};

export default config;
